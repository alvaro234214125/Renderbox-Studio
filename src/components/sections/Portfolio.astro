---
import { Image } from "astro:assets";
import ArrowUpRight from "lucide-static/icons/arrow-up-right.svg?raw";
import Icon from "../ui/Icon.astro";

const projects = [
  {
    title: "Sitio Web Institucional",
    category: "Página Web Profesional & SEO",
    coverImage: "/projects/web-institucional.webp",
    link: "https://inyawaperu.pe/",
  },
  {
    title: "Portafolio Personal",
    category: "Página Web Profesional & SEO",
    coverImage: "/projects/portafolio-personal.webp",
    link: "https://alvarodiazchang.netlify.app/",
  },
  {
    title: "Sistema de Gestión Académica",
    category: "Plataforma Web & AWS",
    coverImage: "/projects/sistema-gestion.webp",
    link: "https://www.campusvirtualinyawaperu.com/",
  },
];

const processSteps = [
    { emoji: "💡", title: "Descubrimiento", description: "Analizamos tus objetivos, el mercado y la audiencia." },
    { emoji: "🎨", title: "Diseño UX/UI", description: "Diseñamos experiencias atractivas y funcionales." },
    { emoji: "💻", title: "Desarrollo", description: "Creamos tu solución con tecnologías modernas." },
    { emoji: "🚀", title: "Lanzamiento", description: "Publicamos y optimizamos tu proyecto." },
];
---

<section id="proyectos" class="py-16 md:py-20 scroll-mt-30 md:pt-0 pt-0 mb-10 md:mb-0">
  <div class="container mx-auto px-6 md:px-20">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-16">
      
      <!-- Columna Izquierda -->
      <div class="md:sticky md:top-24 self-start">
        <!-- Encabezado -->
        <h2
          class="text-4xl md:text-5xl font-bold text-gray-900 leading-tight mb-6 text-center md:text-left"
          data-aos="fade-right"
        >
          Nuestros 
          <span class="inline-flex items-center justify-center bg-indigo-500 rounded-md px-1 sm:px-2 h-12 sm:h-14 text-white pb-2">
            Proyectos
          </span>
        </h2>

        <p
          class="text-lg text-gray-700 max-w-md text-center md:text-left mx-auto md:mx-0"
          data-aos="fade-right"
          data-aos-delay="100"
        >
          Aquí hay una selección de trabajos que demuestran nuestra capacidad para transformar ideas en soluciones digitales robustas y atractivas.
        </p>

        <div 
          class="mt-12"
          data-aos="fade-right"
          data-aos-delay="200"
        >
          <h3 class="font-bold text-gray-800 mb-8 text-lg text-center md:text-left">
            Nuestro Proceso Simplificado
          </h3>

          <div class="relative max-w-md mx-auto md:mx-0 pl-10"> 
            <div class="absolute left-[20px] top-0 h-full w-1 bg-indigo-200 rounded-full"></div>

            <div class="space-y-8 relative">
              {processSteps.map((step, index) => (
                <div
                  class="relative flex gap-4 items-start"
                  data-aos="fade-up"
                  data-aos-delay={index * 150}
                >
                  <div class="absolute left-[-22px] top-1 flex-shrink-0 bg-indigo-500 text-white font-bold h-8 w-8 rounded-full flex items-center justify-center shadow-md z-10">
                    {index + 1}
                  </div>

                  <div class="bg-white/90 backdrop-blur-sm border border-gray-200 rounded-xl shadow-sm p-4 transition-all duration-300 hover:shadow-md w-full">
                    <p class="font-semibold text-gray-900 mb-1 text-lg">
                      <span class="mr-2">{step.emoji}</span>{step.title}
                    </p>
                    <p class="text-sm text-gray-600 leading-relaxed">{step.description}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>

      <!-- Columna Derecha -->
      <div class="flex flex-col gap-8">
        {projects.map((project, index) => (
          <a
            href={project.link}
            target="_blank"
            rel="noopener noreferrer"
            class="group relative aspect-video w-full rounded-2xl overflow-hidden text-left border border-gray-100 shadow-sm hover:shadow-xl transition-all duration-300"
            data-aos="zoom-in"
            data-aos-delay={index * 150}
          >
            <img
              src={project.coverImage}
              alt={`Imagen del proyecto ${project.title}`}
              class="absolute inset-0 w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent"></div>
            <div class="relative h-full flex flex-col justify-end p-8">
              <h3 class="text-3xl font-bold text-white drop-shadow-lg">{project.title}</h3>
              <p class="text-white/80 drop-shadow-md mt-1">{project.category}</p>
            </div>
            <div
              class="absolute top-6 right-6 bg-white/80 backdrop-blur-sm text-gray-900 h-12 w-12 rounded-full flex items-center justify-center transition-all duration-300 opacity-0 group-hover:opacity-100 scale-90 group-hover:scale-100 z-10"
              aria-hidden="true"
            >
              <Icon icon={ArrowUpRight} class="h-6 w-6" />
            </div>
          </a>
        ))}
      </div>
    </div>
  </div>
</section>
